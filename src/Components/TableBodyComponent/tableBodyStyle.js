import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    profilePic:{
        width: 100,
        height: 100,
        marginLeft: 90
    },
    img: {
        height:'100%',
        width: '100%'
    }
})

export default useStyles